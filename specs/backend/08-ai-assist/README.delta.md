# Delta (ajuste mínimo sugerido)
Recomendação: proteger `POST /api/ai/dsl/generate` com policy Reader (ou Admin se preferir restringir).
Este delta não altera o comportamento do AI pipeline além da proteção.
